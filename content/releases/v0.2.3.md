# v0.2.3

Released: April 7, 2026

---

- `npx tsdevstack init` now runs `git init` automatically as part of the scaffold, so the new project starts as a clean git repo without a manual step.
- Template `.gitignore` synced with the framework's own (secrets patterns, observability data dirs, etc.).