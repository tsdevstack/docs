# v0.1.1 – v0.1.4

Released: February 22 – 23, 2026

---

Initial publish workflow stabilisation. Four patch releases over a single day shaking out the GitHub Actions publish pipeline and getting the first packages live on npm.

- GitHub Actions publish workflow iterations (auth, registry tagging, lockfile sync after publish)
- `publishConfig.access: public` set on the unscoped `tsdevstack` package
- One flaky `runCommand` test skipped in CI

No user-facing functional changes — these versions exist purely because each fix needed a published artefact to verify the next workflow step.