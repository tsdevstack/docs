# v0.1.28

Released: March 8, 2026

---

Kong restructure + AWS bootstrap fixes.

- **Kong configuration restructured**: kong-related files split into dedicated config files instead of being bundled into the main compose; eliminates unnecessary kong env-var injection into backend services.
- **AWS bootstrap fix**: `infra:bootstrap` ordering and resource creation fixes.
- **Redirect domains removed** from default infrastructure (were causing avoidable DNS records).
- **Infra constants refactoring**: shared constants pulled into a single module instead of duplicated across providers.
- **Azure ACR pull fix refactored** into a cleaner shared utility.
- AWS setup docs updated.